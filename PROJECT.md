# Dev Lab

Dev Lab is a self-growing developer knowledge base built with Next.js. An automated pipeline picks topics from a backlog, generates practical MDX notes with runnable snippets, validates each change with TypeScript and a production build, and commits only when everything passes.

Browse entries at `/dev`. The goal is honest, useful commits—not dummy timestamp bumps.
